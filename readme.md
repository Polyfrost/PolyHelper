# skyanswers

a bot to do misc things in the SkyClient server, originally intended to only
answer tickets, but extended to replace another bot

## running

1. clone it
2. copy `.env.example` to `.env` and add the needed values to it
3. make sure the bot has edit channel perms
4. `deno task start`
